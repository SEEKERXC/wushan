spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wushan?useSSL=true&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: dev
    password: jdfohewk
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    database-platform: org.hibernate.dialect.MySQL5Dialect

server:
  port: 80